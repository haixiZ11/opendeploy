# DEV_STATE

**更新于 2026-09-07 · 分支 feat/frameless-titlebar（领先 origin 1 个提交，444bc04 未推送）**

当前目标是让无边框标题栏分支（2cefd5b 引入 `frame:false`，移除 Windows 原生标题栏，由应用内 TitleBar 接管，颜色跟随主题）通过合并评审。评审 Blocking 问题是首启向导页隐藏了 TitleBar，而替代它的 wincontrols-float 浮层只是 `position:fixed` 的按钮簇，全页没有任何 `-webkit-app-region: drag` 表面，导致首启用户无法移动窗口。该问题已在 444bc04 修复：向导模式网格预留 40px 的 titlebar 行，由新增的 `.wiz-dragbar` 拖拽条占据（承载最小化/最大化/关闭三键，按钮显式 no-drag 保持可点击），废弃了旧浮层方案——旧方案除无法拖拽外，在小窗口（最小 900×600）下还会遮挡向导卡片的滚动内容。核心文件：`src/main/window.ts`（窗口配置与最大化状态推送）、`src/renderer/App.tsx`（mode-wizard 布局与 dragbar 渲染）、`src/renderer/styles/design-system.css`（.titlebar 与 .wiz-dragbar 的 drag/no-drag 规则）、`src/main/ipc.ts` 与 `src/preload/index.ts`（win:minimize / win:toggle-maximize / win:close / win:is-maximized / win:maximized 五个 IPC 通道）、`tests/renderer/wizard-drag-region.test.ts`（契约测试与手工验证清单）。验证结果：新契约测试 6/6 通过，typecheck 与 lint 干净；全量套件中 `tests/agent/plugin-tools.test.ts`（Windows 临时目录 EPERM ×2）与 `tests/skills/bundle-integrity.test.ts`（common/implementation-planning 与 k3cloud/bos-features-index 两个知识条目清单校验和不匹配，会影响首启种子写入）为分支既有失败，已用 stash 在干净工作区复跑确认与本改动无关。尚未验证的点：`-webkit-app-region` 的 OS 级行为（拖动移动、双击最大化、最大化后拖拽还原、按钮点击、最小尺寸不遮挡）只能人工确认，注意渲染层没有显式的 dblclick 处理器，双击最大化依赖 Windows 系统默认行为，未必生效；验证方法与清单见测试文件注释（设 `OPENDEPLOY_HOME` 指向空目录即可触发首启向导）。下一步：在 Windows 上按清单跑 `pnpm dev` 人工验证拖拽交互，确认后推送 444bc04 并合并；顺手项为修复上述两个既有测试失败；另外工作区有两个未跟踪目录（嵌套的完整项目副本 `opendeploy/` 约 30MB 和 `.workbuddy/`），疑似误复制的产物，提交前需确认是否清理或加入 .gitignore。
